-- This file stores the config for the factoids plugin, mainly the database name
-- Example: Settings = {database_name = "Factoids.db"}
local Settings = {database_name = "Factoids.db"}

function GetFactConfig(name)
  return Settings[name]
end